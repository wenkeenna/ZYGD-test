<UserControl x:Class="ZYGD.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:ZYGD.Views" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:vm="clr-namespace:ZYGD.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:HomeViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <Grid>
        <md:Card  Grid.Row="1"  md:ShadowAssist.ShadowDepth="Depth2" Margin="10" Height="300" Width="200">
            <ItemsControl ItemsSource="{Binding PMatrix}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <i:InvokeCommandAction Command="{Binding NavigateCommand}" 
                                                       CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=ListBox}}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Canvas >
                            <Button Style="{StaticResource MaterialDesignFloatingActionMiniSecondaryButton}"
                        Canvas.Left="{Binding XCorrdinate}" Canvas.Top="{Binding YCorrdinate}" Height="{Binding Radius}" Width="{Binding Radius}" Background="{Binding BackgroundColor}"/>
                        </Canvas>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                
            </ItemsControl>
        </md:Card>
    </Grid>
</UserControl>
